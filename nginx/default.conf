upstream app {
    server host.docker.internal:3000;
}
server {
    listen 80;
    location / {
        proxy_pass http://app;
        proxy_connect_timeout 1s;
        proxy_read_timeout 10s;
    }
}
